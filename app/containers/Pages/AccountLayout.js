import React from 'react';
import { compose, pure } from 'recompose';
import { Container } from 'semantic-ui-react';

function AccountLayout() {
  return <Container>AccountLayout</Container>;
}

export default compose(pure)(AccountLayout);
